# T24 遗留审讯（2026-08-17）

对照 `arch/2026-06-11_project1_rework_plan.md` §四。**只审讯，不在没跑 eval 时删业务代码。**

| 项 | 结论 | 动作 |
|---|---|---|
| `ThinkStreamParser` | **不是死代码**。`chat.py` 仍用来把 Qwen 的 `<think>` 转成前端标记。6-11 说的「LM Studio 前缀去重」不是这个文件现在的职责。 | 留下。已加纯逻辑测试 `tests/test_stream_parser.py`。 |
| `sanitize_assistant_history` | **还在用**。`rag_service.py` 回灌历史前清洗自问自答和行首噪音。当年防 LM Studio 重复生成。要删必须：在当前模型下复现 → 全量 eval → 删 → 再跑对比。 | 本轮不删。 |
| `LMSTUDIO_CHAT_THINKING_FIX_PLAN.md` | 根目录仍在。计划写「并进 commit 说明后删除」。 | 等你提交 T24 时再删，避免和现有脏工作区搅在一起。 |
| `modelscope` | `down_embd.py`、`model_downloader.py` 在用。 | 留下。 |
| `dill` / `peft` / `pyarrow` | 业务代码 **0 处 import**，只钉在 `pyproject.toml`。可能是旧间接依赖的钉子。 | 本轮不删。删之前要 `uv sync` 后跑测试 + eval，确认不是 ultralytics / FlagEmbedding 暗中需要的上限。 |
| `backend/tests/test.py` | 空文件。 | 未动。 |
| `defense_log` | 08-10 已裁定全盘零命中，不再挂账。 | 无。 |

CI 只跑不需要模型和数据库的测试。`test_rag_prompt_constraints` 标 `needs_model`，`test_request_observability` 标 `needs_db`。
