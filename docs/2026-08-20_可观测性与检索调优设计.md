# rework 可观测性与检索调优设计（v2，2026-08-20）

> **版本说明**：v1 经外部模型复审，四条必改点全部核实属实并已修订。
> ⚠️ **其中一条推翻了 v1 的关键假设**：v1 写「纯全文入口需自行核实，可能没有公开 API」，
> 复审指出 `PGVectorStore` 公开支持 `SPARSE`/`TEXT_SEARCH`——**已实测确认，v1 错、复审对**（见 §3.2）。
> 因此 v1 的「方案 B（按 dense/sparse 边界还原 rank）」整节作废，不再需要任何私有方法兜底。
>
> **流程**：本文档 → 外部模型（GPT）复审 ✅ → 修订 ✅ → Grok 执行 → 项目所有者验收。
> **项目定位**：求职作品项目一。目标岗位 AI Agent 后端，2026 年 9 月投递。
> **本设计的判断标准**：**能不能在面试里讲出东西**，不是产品功能完不完整。凡是「加了但面试问不到」的一律不做。
> **仓库**：`/Users/liuchenxu/Documents/Documents/code/rework/`，当前 `main = 41f51b0`。

---

## 〇、立项依据与一条必须先讲的事实更正

### 0.1 为什么是可观测性

[2026-08-14 JD 与面经情报](../../arch/2026-08-14_JD与面经情报.md) 的结论（原文）：

- Kimi JD 把 `Observability` 列进**基本机制**，加分项再列一次 `Trace Analysis`
- 11 项基本机制覆盖 9.5/11，**唯一系统性空白就是 Observability**
- 情报原话：**「可观测性不只是加分，它是失败归因的前提」**

**该缺口在本项目有实证**：2026-08-19 修 eval 检索层三道红题（R06/R09/R10）时，定位靠的是人肉读日志加推测，最后才发现根因是「NFKC 管不到 ⻮」与「FTS 没做中文分词」。**有结构化链路数据的话，这两跳会直接暴露在数据里。**

### 0.2 ⚠️ 一条必须先纠正的事实（本设计的直接起因之一）

**读 llama-index 源码实测**（`llama_index/vector_stores/postgres/base.py:1030` `_hybrid_query`）：

```python
if query.alpha is not None:
    _logger.warning("postgres hybrid search does not support alpha parameter.")
dense_results  = self._query_with_score(...)       # 向量
sparse_results = self._sparse_query_with_rank(...) # 全文
all_results = dense_results + sparse_results       # 直接拼接
return _dedup_results(all_results)                 # 只按 node_id 去重（:1356，全文 7 行）
```

**结论：本项目的混合检索链路里没有 RRF，也没有任何融合排序。** 向量结果在前、全文结果在后，去重时保留先出现的那条。`alpha` 参数被库明确声明不支持。

**已产生的后果**：简历与考纲表此前都写了「RRF 融合」「RRF 权重可复述」——**那个参数不存在，问起来答不出**。简历措辞已于 2026-08-20 更正（详见 §五）。

**这条同时是 P2 的立项理由**：要能讲「融合怎么做的」，就得真的有一个融合实现。

### 0.3 本设计不做什么（先划边界）

| 不做 | 理由 |
|---|---|
| backlog 里的产品功能（任务历史对比 / 用户治理补齐 / 失败任务重试 / 删任务入口） | 纯功能堆砌，面试问不到，占工时不产出叙事 |
| 完整 refresh token 方案 | 批次 2 已裁定「要做就做完整方案，本轮不做」，现在做属翻旧账 |
| 系统设计的「权衡 / 演进路径」缺口 | [考纲表](../../arch/interview_readiness_map.md) 系统设计行实测结论：**这两段不在项目里**，加功能补不到，只能读行业材料 |
| 接入 OpenTelemetry / Jaeger 等外部链路系统 | 引入基础设施依赖，部署与演示成本远超收益；本项目要的是「能讲清自己链路每一跳」，不是「接了个业界标准」 |
| 窄屏适配 | 桌面端求职作品，[全页验收报告](2026-08-20_全页验收报告.md) 已记账不做 |

---

## 一、事实基线（可复核，执行前请自行复验）

### 1.1 已经有的（⛔ 不得重复实现）

| 能力 | 位置 | 状态 |
|---|---|---|
| `request_id` 贯穿全链路 | `app/core/request_context.py`（ContextVar）+ `app/core/logging.py`（LogRecordFactory） | ✅ 完整，深层代码无需传参即可拿到 |
| RAG 分阶段日志 | `app/services/rag_service.py` 共 9 处 `rag stage=` | ✅ rewrite / query / retriever / route / messages / done / ttfb / timeout / cancelled，**均带 ms** |
| **eval 观测口模式** | `chat.py:369-375` + `rag_service.py:464-470` | ✅ **关键**：`RAG_EVAL_DEBUG=1` 时把 `retrieval_debug` 塞进 `meta`，生产零开销 |
| rerank 前后名次表 | `build_debug_nodes`（`rag_service.py:213`） | ✅ 已带 `pre_rerank` / `post_rerank` 全文名次表 |
| eval 取数通路 | `evals/run_rag_eval.py:146` `send_chat` → 从 `message.meta` 取判分依据 | ✅ **trace 只要进 meta，eval 就能拿到** |
| 36 题五层评测 | `evals/eval30_cases.yaml` + `scoring.py` 五个判分函数 | ✅ 当前 36/36 |

### 1.1b 三态清单：已存在 / 待实现 / 待验证（复审必改点 1）

⚠️ **v1 把「meta 通路已存在」与「五跳都已结构化」混为一谈了，这里拆开。**
实测 `grep -n 'result_meta\[' app/services/rag_service.py` 只有 5 类字段：

| 跳 | 已结构化（在 `result_meta` 里） | 只在日志文本里（**待实现**） |
|---|---|---|
| rewrite | `rewritten_query`（且仅多轮时） | 是否跳过、跳过原因、耗时 |
| retrieve | — | mode(hybrid/fallback)、tokenized_query、返回条数、top_score、耗时 |
| rerank | `retrieval_debug.pre/post_rerank`（名次+分数+全文） | **名次变化 `moved`**、耗时 |
| route | `route`、`sources` | context_nodes 数、top_score、injection_score、耗时 |
| generate | `finish_reason` | ttfb_ms、mode、总耗时 |

**结论：现成的是「通路与开关」，不是「数据」。**
五跳中只有 rerank 一跳有较完整的结构化数据；其余四跳的字段**全部需要新写**，
`total_ms` 与逐跳 `ms` 也都不存在（现在只有散落的 `logger.info(... ms=%.1f ...)`）。

**待验证（执行前第一件事）**：`sum(steps.ms)` 与 `total_ms` 的差值构成——
现有日志的 `ms` 都是「从 t0 起算的累计值」（见 `retriever_ms = (time.perf_counter() - t0) * 1000`），
**不是单跳耗时**。做 trace 时必须换成单跳耗时，⛔ 不许直接搬这些累计值。

---

> 🔑 **本设计的核心判断（经复审收窄）**：P1 **复用**已验证的开关与传输通路（`RAG_EVAL_DEBUG` + `meta` + eval `send_chat`），**但五跳的结构化字段绝大部分要新写**（见 §1.1b）。省下的是「设计一套观测体系并接通到 eval」，不是「省下写字段」。

### 1.2 缺口

1. 9 处 `rag stage=` 是**文本行**，无法按 `request_id` 拼成一条链路，无法聚合
2. `retrieval_debug` 只覆盖检索一跳（rerank 前后），**改写 / 路由 / 门卫 / 生成四跳没有结构化数据**
3. eval 结果（`results/*.json`）只有 `pass/fail` 与分数，**没有链路** → 红题无法自动归因
4. 检索超参写死：`RETRIEVAL_TOP_K=10` / `RERANK_TOP_N=5` / `SOURCE_THRESHOLD=-6.0`（`rag_service.py:274-276`，类属性），`CHUNK_SIZE=800` / `CHUNK_OVERLAP=150`（`config.py:75-76`）
5. **没有融合排序**（见 §0.2）

### 1.3 成本基线（实测，决定 P2 可行性）

```
全量 36 题：614 秒（约 10 分钟），单题均 17.1 秒
分层：retrieval 10 题 198s | generation 8 题 142s | guardrail 10 题 112s
      multi_turn 4 题 78s | routing 4 题 84s
```

**数据来源（可复算，⛔ 不是口头预算）**——取自 `evals/results/eval30_20260820_012340.json`：

```bash
cd backend && uv run python -c "
import json,collections
d=json.load(open('evals/results/eval30_20260820_012340.json',encoding='utf-8'))
by=collections.defaultdict(list)
for r in d['results']: by[r['layer']].append(r.get('elapsed_ms',0))
tot=sum(sum(v) for v in by.values())
print('全量 %.0fs  单题均 %.1fs' % (tot/1000, tot/1000/len(d['results'])))
for k,v in by.items(): print('  %-11s %2d 题 %.0fs' % (k,len(v),sum(v)/1000))
"
```

**检索层单跑约 198s（3.3 分钟）** → P2 每组 A/B 对照约 3-4 分钟。
⚠️ 该数字是**串行单跑**的口径；若 P2 改成两路并发取，检索耗时会变，
**对照实验必须在同一口径下重跑基线**，⛔ 不许拿本表的旧数字当基线比。

跑单层命令（runner 已支持，`run_rag_eval.py:320`）：
```bash
cd backend && uv run python evals/run_rag_eval.py --layer retrieval --tag baseline
```

---

## 二、P1：RAG 链路结构化 trace + eval 失败归因

### 2.1 目标

一次问答产出**一条结构化 trace**，覆盖全部关键跳；eval 跑完后，每道红题能直接看出**是哪一跳出的问题**，不必读日志推测。

### 2.2 设计

**开关**：复用既有 `RAG_EVAL_DEBUG`，⛔ **不新增第二个开关**——两个开关会立刻产生「开了一个没开另一个」的组合态。
生产默认关闭；关闭时**除一次 `if` 判断外零开销**，⛔ 不许在关闭时仍构造 trace 对象再丢弃。

**结构**（`meta["rag_trace"]`）：

```jsonc
{
  "request_id": "…",              // 复用 get_request_id()，与日志行可对照
  "total_ms": 8123.4,
  "steps": [
    { "step": "rewrite",  "ms": 412.0, "skipped": true,
      "reason": "no_history",     // 或 "heuristic_skip"
      "original": "…", "rewritten": "…" },
    { "step": "retrieve", "ms": 980.5, "mode": "hybrid",  // 或 "fallback_dense"
      "query": "…", "tokenized_query": "…",               // jieba 分词后的实际 FTS 查询
      "returned": 10, "top_score": -1.23 },
    { "step": "rerank",   "ms": 1500.2, "in": 10, "out": 5,
      "moved": [ { "from": 3, "to": 1 }, … ] },            // 名次变化，用于判断重排是否把正确文档挤下去
    { "step": "route",    "ms": 2.0, "route": "rag",
      "context_nodes": 4, "top_score": -1.23, "injection_score": 0 },
    { "step": "generate", "ms": 5200.0, "ttfb_ms": 1200.0, "mode": "chat",
      "finish_reason": "stop" }
  ]
}
```

**取舍与理由**：

| 决策 | 理由 |
|---|---|
| trace 走 `meta` 而非独立接口 | eval 已从 `meta` 取数（`send_chat:146`），这条路已验证；新开接口要重做鉴权与取数 |
| `retrieval_debug` **保留不动** | eval 检索层判分正依赖它（`scoring.py:132` `score_retrieval` 在 chunk 全文里找 term）。⛔ **改它会打断 36/36 的判分**。`rag_trace` 是**并列新增**，不是替换 |
| trace 里 **不放 chunk 全文** | `build_debug_nodes` 的注释已说明全文会显著放大消息体积；全文由 `retrieval_debug` 承担，trace 只放名次与分数 |
| `moved` 只记名次变化 | 判断「重排把正确文档挤下去」只需要名次，不需要内容 |
| ⛔ 不引入 OTel / Jaeger | 见 §0.3 |

### 2.3 eval 侧消费

1. `run_rag_eval.py` 把 `meta["rag_trace"]` 原样写进单条结果（新增字段 `trace`）
2. 报告新增**红题归因摘要**：对每道非 PASS 用例，打印它的 `steps` 摘要（各跳 ms / 关键字段），⛔ **不做自动"诊断结论"**——那会变成猜测；只把事实摆出来，判断归人
3. `print_report` 增加一段「失败归因」，红题为空时该段不出现

### 2.4 验收（缺一不可）

| 验收项 | 可执行验证 |
|---|---|
| 开关关闭时 meta 无 `rag_trace`，且不构造 trace 对象 | `pytest backend/tests/test_rag_trace.py -k disabled`（断言 meta 无该键 **且** 构造函数未被调用，用 monkeypatch 计数） |
| 开关打开时 trace 覆盖 5 跳 | `pytest backend/tests/test_rag_trace.py -k five_steps` |
| 逐跳 ms 是**单跳耗时**不是累计值 | `pytest backend/tests/test_rag_trace.py -k per_step_ms`（构造已知耗时的假跳，断言不递增） |
| `sum(steps.ms)` 与 `total_ms` 差 < 100ms 且差值可解释 | 同上文件 `-k total_ms_accounted` |
| `request_id` 与日志行一致 | 手动：`RAG_EVAL_DEBUG=1` 跑一问，`grep <request_id> 日志` 应命中该次请求的 `rag stage=` 行 |
| **全量 eval 仍 36/36**（硬闸） | `cd backend && uv run python evals/run_rag_eval.py --tag p1-after` |
| `retrieval_debug` 未被破坏 | `pytest backend/tests/test_rag_trace.py -k retrieval_debug_intact` + 上一条 eval 检索层仍 10/10 |
| 红题能归因 | 手动：临时 `RETRIEVAL_TOP_K=1` 跑 `--layer retrieval`，报告归因段应显示 retrieve 跳 `returned=1` |

---

## 三、P2：实现 RRF 融合 + 超参可配 + eval A/B 对照

### 3.1 目标

把 §0.2 那个「没有融合排序」的事实，变成**一次有数据的改造**：自己实现 RRF，用 eval 量前后差异。

### 3.2 设计

**第一步：把融合从库里接管出来**

✅ **公开入口已实测确认可用**（v1 曾误判为「可能没有」，复审指出并经核实推翻）：

```
llama_index/vector_stores/postgres/base.py:1085-1093
    if   query.mode == HYBRID          -> _async_hybrid_query
    elif query.mode in (SPARSE, TEXT_SEARCH) -> _async_sparse_query_with_rank   # ← 纯全文，公开
    elif query.mode == DEFAULT         -> _aquery_with_score                    # ← 纯向量，代码已在用

llama_index/core/indices/vector_store/retrievers/retriever.py
    :66  vector_store_query_mode 转成 VectorStoreQueryMode 枚举
    :139 作为 mode 传进 VectorStoreQuery
    :92  _needs_embedding() 对 TEXT_SEARCH / SPARSE 返回 False —— 纯全文路径不会计算 embedding
```

⛔ **因此不需要任何私有方法，也不需要 v1 的「方案 B」（已整节作废）。**

**做法**：

1. 纯向量：`as_retriever(vector_store_query_mode="default", similarity_top_k=RETRIEVAL_TOP_K)`
2. 纯全文：`as_retriever(vector_store_query_mode="sparse", similarity_top_k=RETRIEVAL_TOP_K)`，
   查询串用现有的 `tokenize_for_fts(...)`（jieba 分词，`app/services/cjk_fts.py`）
3. 自己做 RRF：`score(d) = Σ 1/(RRF_K + rank_i(d))`，两路按各自名次计分后相加、降序
4. 融合结果进 rerank（这一步不变）

⚠️ **两路并发取**：用 `asyncio.gather`，⛔ 不许串行——串行会把检索耗时翻倍，
而 §1.3 的成本基线是按现状测的，串行会让 P2 的对照数据不可比。

⚠️ **保留降级**：现有「hybrid 返回 0 → 纯向量兜底」的逻辑（`rag_service.py:447-451`）
必须等价保留为「两路都空 → 纯向量兜底」。⛔ 不得因为改融合就把兜底弄丢。

**第二步：超参集中可配**

`RETRIEVAL_TOP_K` / `RERANK_TOP_N` / `SOURCE_THRESHOLD` 从类属性（`rag_service.py:274-276`）
移入 `settings`，与 `CHUNK_SIZE` 等并列；新增 `RRF_K`（默认 60）。
⛔ **默认值必须与当前完全一致**（10 / 5 / -6.0），本步是纯搬家，不得顺手调参。

**第三步：A/B 对照**

`run_rag_eval.py` 增加 `--tag <name>`，写进结果文件名与报告。
⛔ **不做「自动跑多组参数」的批量框架**——脚手架工程，收益低。手动改环境变量跑两次即可。

### 3.3 必须留下的数据

至少三组对照，每组跑**检索层 10 题**（3.3 分钟/组）：

| 组 | 变量 |
|---|---|
| 基线 | 当前行为（拼接去重） |
| RRF k=60 | 融合改 RRF |
| RRF k=10 / k=100 | 看 k 的敏感度 |

**结果写进 `docs/2026-08-20_检索调优对照.md`**，含每组的 `hit@5` / `hit@10` / `MRR`。
⚠️ **如果 RRF 没有带来改善，如实记录并保留基线行为**。⛔ 不许因为「做了就要用」而强行切换——「我实现了 RRF，实测在本题库上与拼接去重无显著差异，所以没上」同样是好答案，且更可信。

### 3.4 验收

| 验收项 | 可执行验证 |
|---|---|
| RRF 纯函数正确（给定两路 rank 验证输出顺序） | `pytest backend/tests/test_rrf_fusion.py -k ranking`，⛔ 不依赖真实检索 |
| 两路并发而非串行 | `pytest backend/tests/test_rrf_fusion.py -k concurrent`（用假 retriever 记录调用时序） |
| 两路都空时降级为纯向量（既有兜底不丢） | `pytest backend/tests/test_rrf_fusion.py -k fallback` |
| 超参搬家后默认行为完全一致 | 搬家提交单独跑 `uv run python evals/run_rag_eval.py --tag param-move`，必须仍 36/36 |
| 三组对照数据齐备 | `evals/results/eval30_*_baseline.json` / `*_rrf-k60.json` / `*_rrf-k10.json` / `*_rrf-k100.json` 均存在，且写入 `docs/2026-08-20_检索调优对照.md` |
| 基线在同口径下重测 | 对照文档须注明基线是**并发改造后**重跑的，⛔ 不许用 §1.3 的旧数字 |
| 是否启用由数据决定 | 对照文档写明结论与理由；⛔ 无改善就保留原行为并如实记录 |

---

## 四、P3：评测与链路可视化页面

### 4.1 目标

前两条的产出都只存在于 JSON 文件里。**面试官不会翻 `evals/results/*.json`。** 做一页让它可见。

### 4.2 设计

**路由**：`/evals`，放在管理区（与知识库三页同级），仅 superuser 可见。

**数据来源（二选一，执行方先说选哪个及理由）**：
- ⑴ 新增只读接口，后端扫 `evals/results/*.json` 返回列表与详情
- ⑵ 构建期把结果拷进前端静态资源

**倾向 ⑴**，理由：⑵ 会让页面数据与仓库状态脱节，且每次跑完 eval 都要重新构建。

⚠️ **路径安全必须落到既有口径上，⛔ 不许另起一套**（复审必改点 4）：

仓库已有 `app/core/static_paths.py:36 resolve_existing_file()` —— 候选路径逐个
`.resolve()` 后用 `_is_within(resolved, STATIC_DIR)` 判边界。**但它锚定的是 `STATIC_DIR`，
而 eval 结果在 `evals/results/`，不在 static 下。**

**定案**：把 `static_paths.py` 里的 `_is_within` 提为共享判据，
新增 `resolve_eval_result(name) -> Path | None`，锚定 `EVALS_RESULTS_DIR`，
**复用同一套「resolve 后判 within」的逻辑与同样的失败语义（返回 None → 404）**。
⛔ 不得新写一套字符串黑名单式的过滤（如判断有没有 `..`）——那与既有口径不一致且更弱。

**鉴权落点**：走既有 `AdminRoute` 对应的后端依赖，与知识库三页同一条路径；
非 superuser 一律 404（不是 403，与 §2.2 既有的「不泄露资源存在性」口径一致）。

**页面内容**：
1. 历次跑批列表（时间、总分、分层得分）
2. 单次详情：五层通过率 + 36 题表格
3. 点开单题：判分明细（`scores`）+ **链路 trace（P1 的产出）**
4. 红题高亮

### 4.3 验收

| 验收项 | 可执行验证 |
|---|---|
| 非 superuser 返回 404 | `pytest backend/tests/test_eval_report_api.py -k forbidden` |
| 路径穿越被拒（含 `../`、绝对路径、软链） | `pytest backend/tests/test_eval_report_api.py -k traversal` |
| 复用 `_is_within` 而非新写过滤 | `grep -n "_is_within" backend/app/core/static_paths.py backend/app/services/*.py` 必须命中共享实现 |
| 页面展示 36 题与至少一条 trace | `pnpm test:run src/pages/EvalReportPage.test.jsx` |
| 列表渲染 / 红题高亮 / 空态 | 同上文件内三条用例 |

---

## 五、简历口径更正（已执行，记录在案）

⚠️ §0.2 的事实使简历三处措辞失真，**已于 2026-08-20 更正**（`arch/resume/_body.html`）：

| 位置 | 原文（不实） | 现文 |
|---|---|---|
| 项目二·动作 | 「pgvector 与 PostgreSQL 全文检索并行、**RRF 融合**后接 rerank」 | 「混合检索（pgvector 向量 + PG 全文），**中文预分词、多轮改写与降级兜底自研**」 |
| 项目二·结果 | 「检索、融合、重排三段**全部手写而非调用框架封装**，chunk 大小、**RRF 权重**、top-k 的取舍逐项可复述」 | 「自建 **36 题五层评测**，现 36/36。检索三题长期红，量化定位到两处入库缺陷…修复后**检索 7/10 → 10/10**」 |
| 核心能力·RAG | 「pgvector + 全文检索 + **RRF 融合** + rerank，超参取舍逐项可说明」 | 「pgvector + 全文检索 + rerank；**36 题五层评测**驱动调优，每次改动前后都有对照数据」 |

**PDF 已重新生成并校验**：仍为 1 页，全文不再出现 `RRF` 与「全部手写」。

📌 **若 P2 最终启用了 RRF**，简历可再次升级为「读库源码发现 hybrid 只是拼接去重，自行实现 RRF 并用 36 题量化前后差异」——**那时才是真的，且比原措辞更强**。

---

## 六、执行纪律（给 Grok）

沿用包 A-F 的既有约束，本包额外强调：

1. **一件一 commit**，说明写「问题 → 修法 → 测试」
2. **P1 → P2 → P3 顺序执行**，P2 依赖 P1 的 trace，P3 依赖前两者
3. ⛔ **数据不伪造不美化**：RRF 无改善就如实写，红题就是红题
4. ⛔ **先量后改**：P2 的每组对照必须有实测数字，不得估算
5. **核查类 grep 不许带 `head`**，正则匹配同一句式时不许只取首个（本项目两次栽在这上面）
6. 🔴 **停下上报的情形**：①任何改动导致全量 eval 跌破 36/36 ②**发现自己需要调用库的私有方法**（`_` 开头）——公开模式已够用，需要私有方法说明方向错了 ③实测与本文档 §一 事实基线不符
7. **每个阶段结束跑全量 eval**（10 分钟），⛔ 不许只跑检索层就宣布收工
8. 从 `main` 开工作分支，走 PR，**不许直接推 main**；push 后确认 CI 双绿

## 七、风险与止损

| 风险 | 应对 |
|---|---|
| trace 拖慢生产链路 | 开关默认关闭 + 关闭时零构造；验收含「关闭时 meta 无该键」 |
| trace 改动打断既有判分 | `retrieval_debug` 保留不动，trace 并列新增；硬闸=全量仍 36/36 |
| RRF 接管融合后检索变差 | 三组对照数据决定是否启用；不启用也是有效交付 |
| ~~库无公开全文入口~~ | ⛔ **该风险已消除**：`SPARSE`/`TEXT_SEARCH` 实测为公开分派（§3.2），v1 的方案 B 作废 |
| P3 读文件产生路径穿越 | 白名单 + 专门的穿越测试 |
| 三项都做不完 | **优先级即执行序**：P1 单独完成即可交付（它才是 JD 点名的空白）；P2、P3 是增强 |


---

## 八、P1 验收记录（2026-08-20 架构台实测）

**结论：通过。** PR #5 / CI `32359604549` 双绿（backend 8m50s + frontend 1m39s），未合 main。
交付 commit `74f77e8`，结果 `evals/results/eval30_20260820_183258_p1-after.json`。

### 8.1 硬闸与两项重点核查

| 项 | 实测 |
|---|---|
| **全量 eval 36/36**（非只跑检索层） | ✅ retrieval 10/10（MRR 0.824）· generation 8/8 · guardrail 10/10 · multi_turn 4/4 · routing 4/4 |
| **关闭时零构造**（架构台预告要查） | ✅ monkeypatch 计数实测：关闭时 `RagTrace` 构造 **0 次**、返回 `None`；打开时 1 次。⛔ 不是「有键但没用」 |
| **逐跳 ms 是单跳非累计**（架构台预告要查） | ✅ 扫全部 31 条 trace：**单调递增者 0 条**；`sum(steps.ms)` 与 `total_ms` 最大差 **0.90ms**。R01 形状 `0.0 → 837.8 → 1548.7 → 2.2 → 10453.4`，`route` 仅 2.2ms 夹在两个大值之间——累计值不可能是这个形状 |
| `retrieval_debug` 未被破坏 | ✅ 检索层仍按 chunk 全文判分，10/10 |
| `request_id` 与日志对得上 | ✅ `b8dcfdb268da410b8f4ee61ed2f4397e` 可在同请求的 `rag stage=` 行命中 |

### 8.2 执行方主动坦白的两条，均核实属实

1. **GR01-GR05 无 trace**：门卫处直接拒答，未进 RAG 五跳；GR06-GR10 五跳齐全。
   **设计结构里本就没有 guardrail 这一跳，未擅自补**——正确，改结构应回来改设计。
2. **R02 的 rewrite 跳 15016ms**：评测共用账号，R01 一问即产生历史，之后每题被门卫判为多轮。
   **旧行为，trace 只是把它暴露出来，非本次引入。**

### 8.3 🎤 trace 上线当天就兑现了价值（架构台顺着第 2 条往下量）

```
rewrite 跳过的题:  1 道
rewrite 真跑的题: 30 道        ← 31 道里 30 道都在跑改写
  耗时 min 4.3s / 中位 6.7s / max 15.0s
rewrite 累计 222 秒，占全部链路总时长 565 秒的 39%
```

**评测里近 40% 的时间花在多轮改写上，而这些题绝大多数不是多轮题。**

🪝 **与 2026-08-18「40 请求共用会话窗口被污染」是同一个病的第二次发作**：
那次暴露的是**内容污染**（`rewritten_query` 从「腐蚀」错成「隐裂」），这次是**耗时污染**。

**该数字此前无人可见**——日志里只有孤立的 `rag stage=rewrite ms=...`，没有人会把 30 行加起来。
**这正是「可观测性是失败归因的前提」的实例，且由本项目自己产生，不是引述 JD。**

### 8.4 ⚠️ 一条验收项未执行（如实记录，不作豁免）

设计 §2.4 的「临时 `RETRIEVAL_TOP_K=1` 跑一次，看归因段能显示 retrieve 跳异常」**未执行**。
执行方理由是「硬闸已用全量 36/36 收口」——**该理由不成立**：这条验的是**归因段本身可用性**，
而全绿时归因段按设计根本不出现，等于**归因功能至今未被验证过一次**。

**处置**：不影响 P1 通过（归因代码在、单测覆盖 FAIL 分支），但**欠账转入 P2**：
P2 改检索链路时天然会产生红题，届时顺手验一次，⛔ 不得再以「全绿所以没出现」结案。

### 8.5 对 P2 的两条约束（本次验收新增）

1. **归因段验收欠账** → 见 §8.4，P2 出现红题时补验。
2. 🔴 **A/B 对照的耗时口径必须排除 rewrite**：
   rewrite 现占链路总时长 **39%**（§8.3）。若对照只看「总耗时」，检索改造的效果会被它淹掉。
   **对照必须看 `retrieve` + `rerank` 两跳的耗时**，⛔ 不许用 `total_ms` 或 eval 总时长做检索性能对照。


---

## 九、P2 验收记录（2026-08-20 架构台实测）

**结论：通过。「不启用 RRF」是对的。** PR #8 / CI `32365510263` 双绿。

### 9.1 硬闸与纪律

| 项 | 实测 |
|---|---|
| 超参搬家 `--tag param-move` | ✅ 36/36（纯搬家未改行为） |
| 收口 `--tag p2-after` | ✅ 36/36 |
| ⛔ 未用库私有方法 | ✅ `grep '_sparse_query\|_hybrid_query\|_dedup_results'` 零命中 |
| 默认保留原行为 | ✅ `RAG_FUSION_MODE = "concat"` |
| 两路并发而非串行 | ✅ `asyncio.gather`；实测 retrieve 跳 226ms → 160ms（**−29%**） |
| 两路都空走纯向量兜底 | ✅ 保留 |

### 9.2 「不启用」的论证，架构台复核成立

⚠️ **架构台第一次估噪声时把 jieba 修复前的旧数据混了进去**（极差 0.240），那不是噪声是真实改善。
按代码版本重新分组才对：

```
同一行为(concat)四次重复：0.824 / 0.757 / 0.774 / 0.761  → 自身极差 0.067
RRF 最高 0.835 − concat 最高 0.824                       → 效应量 0.011
```

**效应量 0.011 远小于自身噪声 0.067**，不足以支持「RRF 更好」。实现留下、默认不启用，正确。

### 9.3 🎤 架构台补的机制解释（比「抖动」更能回答面试追问）

**k=10 与 k=100 的 MRR 完全相同（均 0.835）不是巧合**：逐题看 **8/10 完全相同**，仅 R01/R02 恰好互换、均值才碰巧相等。

根因：`RRF score = Σ 1/(k+rank)`。两路各取 top_k=10 共 20 条，去重后 13–17 条，
**重叠仅 3–7 条（15%–35%）**。对只在一路出现的文档（占多数），分数只有一项，
排序完全由 rank 决定、**与 k 无关**；k 只作用在那 3–7 条重叠文档上，样本太少被稀释。

> 面试可答：「k 在我这个题库上不敏感，因为两路重叠只有 15%–35%。RRF 的 k 是调节
> 『多路命中』相对『单路高名次』权重的，重叠低时它没有发挥空间。」

### 9.4 自研 concat 与 llama-index hybrid 的实际差异

**行为等价**（融合方式、两路 top_k、去重依据均一致），差异有四：
①串行→并发（−29%）②从黑盒变可观测（记 `dense`/`sparse`/`overlap`）③可切换融合策略 ④降级条件语义更精确。

---

## 十、P3 验收记录（2026-08-21 架构台实测）

**结论：通过，三件欠账全部收口。** PR #9 / CI `32391011110` 双绿，已合 `main = fe0c9d7`。

### 10.1 三件欠账

| 欠账 | 结清方式 |
|---|---|
| **归因段现场验证**（挂了 P1、P2 两轮） | ✅ `RETRIEVAL_TOP_K=1` 造出 5 道红题（`p3-attr-k1.json` 5/10），报告打出「失败归因」段。R02 的 retrieve 跳显示 `dense=1 sparse=1 overlap=0 returned=2` —— 一眼看出检索跳被掐死。**不再是「全绿所以没出现」结案** |
| `dense`/`sparse`/`overlap` 字段 | ✅ 已记；默认 k 那次 overlap 4–7（20%–35%），与架构台当初 `20−returned` 反推一致 |
| k 机制写入文档 | ✅ |

硬闸：`p3-after` 36/36、`dense-nl-full` 36/36。

### 10.2 dense 喂错查询串：判断成立，改动留下

**问题**（架构台 P2 验收时发现，非本轮引入）：`hybrid_query` 是 jieba 空格串，
分词是给 **sparse/PG 全文**用的；向量模型要自然语言。而 fallback 那路向量一直吃 `augmented_question`（原句），
**同一种检索两处喂法不一致**。

**改法**：`dense → augmented_question`（原句）、`sparse → hybrid_query`（分词串）。

**对照**：改前 MRR 0.830 → 改后 0.769，**看似变差**。执行方仍决定留下，理由
「这是喂错模态，不是调参；MRR 没抬上去不等于该继续把空格串喂给向量」。
**架构台复核：该判断成立，且有更硬的证据（见 §10.3）。**

### 10.3 🔴 本轮最大发现：R01/R02 是这套题库的两颗噪声源

跨 **9 次跑批**逐题比对：

```
题号   p1-aft param- baseli rrf-k60 rrf-k10 rrf-k100 p2-aft p3-aft dense-nl
R01    0.390  0.390  0.390  0.390   1.000   0.500    0.390  0.500  0.500   ← 波动 0.61
R02    1.000  0.330  0.500  0.500   0.500   1.000    0.440  1.000  0.330   ← 波动 0.67
R03-R09  ——— 九次跑批分毫不动 ———
R10    0.880  0.880  0.880  0.830   0.880   0.880    0.810  0.830  0.830   ← 波动 0.07
```

**10 道题里 7 道完全不动，全部波动只来自 R01/R02。**

**根因（架构台再查一层）**：这两题的三个判分词**挤在同一个 chunk 里**——

```
R01  term_ranks = {敲击法:2, 超声波:2, 热像仪:2}        ← 三词同 chunk
R02  term_ranks = {V型开槽:3, 环氧树脂:3, 玻璃纤维布:3}  ← 三词同 chunk
R05  term_ranks = {0.1m²:1, 3mm:1, 停机:1}             ← 稳定题，目标稳在第 1
```

MRR 完全由**那一个 chunk 排第几**决定：第 1 名 1.0 / 第 2 名 0.5 / 第 3 名 0.33。
**名次动一格，分数跳半档。**

**推论**：0.830 → 0.769 实质是 R02 的目标 chunk 从第 1 掉到第 3，
而同一配置下它自己就在 0.33–1.00 间跳。**该对照实验在 R01/R02 上没有分辨力**；
数据既然无法区分，就该按原理判断 —— **「改动留下」正确**。

⚠️ **同时意味着**：此前 RRF 对照、k 值对照的分辨力**都被这两题吃掉了**。

### 10.4 📌 记账（⛔ 本轮不改）

**R01/R02 判分词过于集中，使检索层 MRR 长期不可比。**
修法：把判分词分散到多个 chunk，或对这两题改用 hit@k 而非 MRR。

⛔ **不该现在改**：改判分词＝改尺子，会让此前 9 次跑批数据全部不可比。
**留待下一轮评测扩题时一并处理。**

---

## 十一、三件收官小结

| | 交付 | 面试可讲的一句 |
|---|---|---|
| **P1** | RAG 五跳结构化 trace + eval 红题归因 | 「上线当天就查出：31 题里 30 题在跑多轮改写，占链路总时长 39%，而它们绝大多数不是多轮题」 |
| **P2** | 自研 RRF + 两路并发 + 三组对照 | 「读库源码发现 hybrid 只是拼接去重。我自己实现了 RRF，量了三组数据，**没改善所以没启用**」 |
| **P3** | `/evals` 可视化 + dense 喂错模态修复 | 「所有 MRR 波动都来自两道判分词过于集中的题——**我的尺子在这两题上没有分辨力**」 |

**三件的共同底色：不是「我加了功能」，是「我量了，然后据此决定做或不做」。**
