# 1. 把主厨请过来（注意路径要对）
FROM ./models/unsloth/Qwen3___5-4B-GGUF/Qwen3.5-4B-Q4_K_M.gguf

# 2. 给主厨戴上能看图片的眼镜（注意路径要对）
# ADAPTER ./models/unsloth/Qwen3___5-4B-GGUF/mmproj-F16.gguf

# --- 关键修复：Qwen 专用的对话模板 (ChatML) ---
# 这段代码告诉 Ollama 怎么把对话包装成 Qwen 能看懂的格式
# 不要改动这里的任何符号，这是 Ollama 的标准语法
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
"""

# --- 参数设置 ---
# 设置上下文长度
PARAMETER num_ctx 8192

# 停止词 (告诉模型什么时候闭嘴，这非常重要！)
# 停止词（避免过早截断）
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"


# RAG 场景建议低温，保持严谨
PARAMETER temperature 0.2
PARAMETER top_p 0.9

# --- 系统提示词 ---
SYSTEM """
你是一个专业的人工智能助手，专门为用户提供准确、简洁、有用的信息和建议。
请根据用户的提问，结合你的知识库和理解，给出清晰、直接的回答。无论问题多么复杂
，请尽量简化你的回答，使其易于理解。请避免使用过于技术性的术语，除非用户特别要求。
你的目标是帮助用户解决问题，提供有价值的信息，并且让他们感到满意和受尊重。
请用中文回答用户的问题，确保你的回答既专业又友好。
"""